export class Tokenization {
  email: string;
  expiration_year: number;
  expiration_month: number;
  cvv: string;
  card_number: string;
  token: string;
  pk_token: string;
  type_card: string;
  state?: boolean;
}